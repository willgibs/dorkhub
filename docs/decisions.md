# Decisions (append-only, one dated line each)

- 2026-07-21 · v1 = full shaped scope (profiles, GitHub connect, project pages, feed, like/save/follow); featured-slot monetization deferred but schema anticipates it.
- 2026-07-21 · Cold start = seed + open launch: admin-seeded unclaimed profiles; claim authorized ONLY by immutable GitHub numeric id match in one atomic UPDATE; invite tokens are UX, never authorization.
- 2026-07-21 · Stack: Next.js 16 / Tailwind v4 / shadcn / Supabase / Vercel / Biome / pnpm. Single app, route groups, no monorepo.
- 2026-07-21 · Auth-decoupled profiles: profiles.id standalone uuid, nullable unique user_id → auth.users; enables pre-claim seeded content.
- 2026-07-21 · GitHub provider_token deliberately unused — one server PAT (public read-only) for ALL GitHub reads; Supabase OAuth = identity only.
- 2026-07-21 · Trending = write-time Reddit-style score (log10 engagement + epoch/45000); indexable, no cron.
- 2026-07-21 · Screenshots never touch Vercel image optimization: client-side resize → WebP renditions → Supabase public bucket → unoptimized serving (top cost trap on both platforms).
- 2026-07-21 · Design locked: "quiet dev-native" (01+02 hybrid) — Instrument Sans / Geist / JetBrains Mono, ice-cyan accent, charcoal neutral, 0.45rem radius, dark-first. Refs: paper.design, resend.com, basehub.com, cosmos.network.
- 2026-07-21 · Micro-details are intentional product surface (Will: "minor design details make a product feel special") — preserve, don't minimalize.
- 2026-07-21 · DB on hold: Supabase free slots 2/2 (hopper, qrcdn); Will declined pausing either or upgrading. SQL ships in-repo, unapplied.
- 2026-07-21 · Frugality: execution subagents run sonnet/haiku; Fable orchestrates. TypeScript pinned to 5.x (7.0 preview breaks Next 16 build worker).
- 2026-07-21 · Vercel Hobby during build; Pro ($20/mo) at public launch (Hobby ToS is non-commercial); Spend Management on day one of Pro.
- 2026-07-21 · `/design/components` styleguide rule "fed ONLY by fixtures+copy" applies strictly to the 24 design-system components; raw shadcn primitives (Button, Select, etc.) may use self-describing labels (e.g. a button reading "outline") since they carry no product voice of their own.
- 2026-07-21 · `.claude/launch.json`'s `app` dev-server config got `"autoPort": true` — port 3000 is frequently held by a sibling project (qrcdn) on this machine.
- 2026-07-21 · transitions.dev sourcing: free tier, adapt-don't-vendor (terms forbid redistributing the library; re-implement techniques on our tokens). Pro not purchased.
- 2026-07-21 · emilkowalski/skills (MIT) vendored project-scoped at .agents/skills/ (+.claude/skills symlinks) and committed; refresh = deliberate diff. emil-design-eng required reading for animation work; review-animations = motion QA gate.
- 2026-07-21 · Motion stack: CSS-first + tw-animate-css for overlay enter/exits; `motion` only in 'use client' leaf islands (LazyMotion+domAnimation). Latent bug found: 6 shadcn primitives shipped dead animate-in/out classes (no animation plugin) — fixed in M2.5.
- 2026-07-21 · M2.5 motion retrofit judgment calls: Dialog/DialogContent gained an `animated` prop (default true) so CommandDialog can pass `animated={false}` — the ⌘K palette shares Dialog's markup but the high-frequency-action rule bans its animation, verified in-browser that the command surface renders with zero animate/duration classes while every other Dialog consumer keeps them. AvatarStack's hover-return overshoot uses an explicitly-motion.md-sanctioned custom cubic-bezier (the one exception to the two named easings). "Adapted from transitions.dev" note on /design/motion scoped to the 6 overlay primitives only, not the whole page, since the rest (StatButton burst, AvatarStack overshoot, etc.) are original.
- 2026-07-21 · ui/collapsible grid-rows retrofit: needs `forceMount` on CollapsibleContent (else Radix unmounts on close before the CSS transition can play) and the animating grid element's className must stay fixed — never merged with a consumer's own className, since a bare `flex` there silently overrides `grid` and kills `grid-template-rows` entirely (any consumer spacing must live on an inner wrapper instead). Shipped broken on the first pass; caught via live browser DOM inspection (computed `display`/`grid-template-rows`), not by `pnpm verify`/`build` (both are blind to this — it's a runtime CSS-cascade bug, not a type or lint error).
- 2026-07-21 · `ui/button.tsx`/`ui/tabs.tsx` `transition-all` (motion.md violation) fixed same-day with scoped transition-[…] lists; review-animations gate then drove: global `@custom-variant hover` gating (touch-safe hovers everywhere), stat-burst onto `--motion-slow`, staggered number pop-in, collapsible cost-comment correction. Accepted simplification: hover color transitions reuse ease-quiet (two-curve system) rather than adding a third `ease` curve.
- 2026-07-22 · Route groups (marketing)/(app) adopted pre-auth (URLs unchanged); home sections are page-local _sections/, not design-system components.
- 2026-07-22 · robots: noindex sitewide until M9 (draft copy + non-canonical vercel.app domain); one-line flip at launch.
- 2026-07-22 · OG: static brand card + og-tokens.ts hex mirrors sampled from live computed styles (satori can't parse oklch); dynamic per-project/profile OG deferred to M4+ on the same toolchain. Fonts vendored (OFL statics).
- 2026-07-22 · Footer: /colophon folded into /manifesto#colophon; dead github.com/dorkhub link removed.
- 2026-07-22 · Vercel: file-tree MCP deploy rejected (token-prohibitive, no git link); Will imports the repo in the dashboard — project reused for M9 Pro+DNS.
- 2026-07-22 · sanitize: mailto stripped (https-only schemes), protocol-relative passthrough, scheme-regex exclusion before rewrite (superset guard). cursor: decode never throws.
- 2026-07-22 · Manifesto FINAL (product gate): merged take — A's calm register + B's strongest lines (forking/shipping/steal tenets from B); closer 'go build a thing.' Switcher + losing draft deleted.
- 2026-07-22 · DB lives in a DEDICATED Supabase account/org ("Dorkhub Team", fresh free tier) — Will's main org + its MCP stay reserved for other agents. Access = Management API PAT + psql over session pooler (both via .env.local); no MCP.
- 2026-07-22 · projects_before_update() guard shipped in 0001 (jsonb-minus-counters comparison): likes/saves/trending recounts no longer bump updated_at; verified behaviorally.
- 2026-07-22 · Advisor pass → 0002 hardening: revoked API-role EXECUTE on trigger/event functions, pinned compute_trending search_path. Accepted-by-design (documented in 0002): claim_invites deny-all, public screenshots bucket, current_profile_id executable by API roles (RLS needs it).
- 2026-07-22 · Usernames LOCKED to GitHub logins (Will: no impersonation) — onboarding customizes only display_name/bio (skippable). supabaseService() now fail-loud validates the key shape; onboarding insert errors are logged server-side (prod failure was silent — suspected wrong SUPABASE_SECRET_KEY value in Vercel env).
- 2026-07-22 · PROD BUG (first real sign-up): 0001's revoke-all stripped service_role's DML on all public tables → onboarding INSERT 42501, masked by the generic error branch. Fixed by 0003 (grant all + default privileges); rls_checks now asserts service_role DML so it can't regress. Vercel env vars were innocent.
- 2026-07-22 · Avatars: GitHub avatar pulled as default (like display_name), editable at onboarding via upload → client-resized 256px WebP → dedicated public avatars bucket keyed by auth.uid() (uploads pre-date the profile row). avatar_url host-allowlisted server-side (githubusercontent or our bucket only). Rendered as plain <img> everywhere per cost rules.
- 2026-07-22 · First-user QA: onboarding redirect reused the SHARED layout from the client router cache → stale pre-signup header ('?' avatar). Fix: revalidatePath('/','layout') before the post-signup redirect. DB/grants were innocent (verified before touching code).
- 2026-07-22 · M4 GitHub layer: plain fetch over octokit (3 endpoints; injectable fetchImpl → dependency-free tests with real Response objects; zero new runtime deps). Sync fetches by NUMERIC repo id (/repositories/{id} — renames self-heal); slug never rewritten by sync (dorkhub URLs permanent). last_synced_at bumps on ok/304/404, never on rate-limit/error — else dead/unchanged repos pin the cron queue front and starve rotation.
- 2026-07-22 · M4 scope cuts: description_md authoring/rendering → M6 (keeps unified/rehype stack out; README is the page centerpiece). Repo-deleted indicator column → M8/M9. Dynamic per-project/profile OG SHIPPED in M4 (hashed-path serving: Next embeds /…/opengraph-image-<hash> URLs into page meta automatically; miss → deterministic brand-card fallback, never a 404).
- 2026-07-22 · Project pages read via cookie-bound supabaseServer(): tree is dynamic anyway (SiteHeaderSession) and RLS published-or-own shows owners their fresh drafts free. Owner edits (tagline/tags/demo_url/status/sort_order) go through RLS under the column grants; creation + sync writes are service-role only. revalidate=300 + revalidatePath kept (inert today, correct after M5 caching pass).
- 2026-07-22 · GithubConfigError (missing GITHUB_TOKEN) degrades: /new → quiet empty state, createProject/syncProject → error outcomes, cron tallies + retries. Loud in logs, never a 500. Verified live pre-PAT: cron 200 {batch:9, errored:9}.
- 2026-07-22 · docs/plans/ convention (per Will): each milestone's approved plan is distilled into docs/plans/mN-*.md; subagents onboard from the repo, not compacted chat context. vitest aliases 'server-only' → its shipped no-op stub (what Next substitutes in prod). Known M6 polish item: /new nests interactive controls inside cmdk CommandItem (keyboard a11y worth a pass).
- 2026-07-22 · SEED HAZARD caught pre-token: fixture github_repo_ids sat in GitHub's REAL id range — first live cron would have resolved them to strangers' repos and overwritten fixture rows. Shifted +9e11 (seed.sql + live DB); sync now cleanly not_founds them (keeps data, round-robins). Live E2E with real PAT: synced octocat/Hello-World (metadata+sanitized README+ETags), second run 304 not_modified, seeds preserved.
- 2026-07-22 · M5 caching pass: layouts are cookie-free; auth slot = client island (src/app/_shell/site-header-auth.tsx) resolving via supabaseBrowser on mount + every pathname change — stale-header bug class dead by construction; /, /manifesto, /design/*, /trending, /tags build static/ISR. Signed-in home = proxy REWRITE / → /home reusing the existing claims check (URL stays /); the authed rewrite must copy refreshed session cookies onto the new response or token refreshes drop.
- 2026-07-22 · M5 interactions: likes/saves/follows write DIRECTLY from the browser client under RLS (grants designed for it) — optimistic flip, 23505 = success, revert on other errors, per-id pending guards; counts self-heal via triggers; writers never revalidatePath (60s feed / 300s page ISR absorbs it). Feed sort/tag live in URL paths (searchParams would force dynamic). Load-more = server action returning rendered ReactNode (ProjectCard stays RSC; likeSlot?: ReactNode replaced liked/onLikeToggle).
- 2026-07-22 · PGRST201 caught pre-mount by probing live PostgREST: projects↔profiles has 3 relationships (FK + m2m via likes/saves) so feed embeds MUST name the FK (profiles!projects_profile_id_fkey!inner). The FeedRow type cast would have hidden this until runtime — probe embeds live before trusting select-string casts.
- 2026-07-22 · PRODUCT (Will, post-M5 QA): curator-first strategy — the silent majority are consumers/curators; weight their UX equally with creators'. Flywheel: broad ingestion + admin approval (admins = Will + orchestrator only) fills the gallery → curation traffic → real builders list. "Slow monetizable growth over 15 minutes of fame." Importer platform (maybe a separate admin app) gets its own planning round after M5.5; it reshapes M8's manual seed tooling into ingestion→approval-queue.
- 2026-07-22 · M5.5 search: NO PostgREST .or() anywhere in search (its filter grammar parses , and () — an injection surface beyond SQL ILIKE wildcards); paired .ilike() legs + pure merge/rank instead. Palette hand-composes Dialog+Command (CommandDialog can't pass shouldFilter; ui/ primitives never edited); trigger↔dialog share a module-level store via useSyncExternalStore. Header flex-wrap fix also cured prod's pre-existing clipped sign-in at 375px. profiles.username trgm index kept despite tiny-table planner preference (expression matches; wins at scale).
- 2026-07-22 · GOVERNANCE + VISION (board-approved): Will = board (vision, featured-slot pricing, launch go, new spend, consent escalations); orchestrator = CEO executing to launch. Vision in docs/vision.md — "find tools they weren't looking for"; discovery over lookup (search demoted to utility); stars import = activation ("bring your stars"; GitHub Lists have no API — excluded, no scraping); quality is the cap (human approval = volume throttle, nothing auto-publishes); pixel-perfect launch bar. Roadmap P1 gallery engine → P2 discovery mechanics → P3 rich pages/design QA → P4 featured slots + hardening + dorkhub.com.
- 2026-07-22 · P1 architecture (docs/plans/p1-gallery-engine.md): ingest_candidates keyed by github_repo_id (numeric-only rule), approval=publish with fresh re-fetch, N repos/owner → 1 unclaimed profile, retroactive save materialization from star_imports, sticky rejections with demand still accruing, admin-triggered crawls only (search API = separate 30/min bucket → sequential throttle), ingest_blocklist consent gate on every path, star_imports own-rows RLS (no FK — dual-target by design), everything else deny-all behind requireAdmin()+service role.
- 2026-07-23 · P1 implementation notes: direct-published inserts must stamp published_at + trending_score explicitly (projects_before_update trigger is UPDATE-only); blocklist upserts swallow 23505 instead of ON CONFLICT (PostgREST can't target partial unique indexes); decline-claim marker = claim_invites.used_at repurposed (any used row = declined; shared predicate across callback//claim/admin — no schema change, no sign-in nag); upsertCandidates preserves decided rows and keeps source sticky to the first discoverer; saves-from-import filtered to published targets (RLS insert policy requires it).
- 2026-07-23 · P1.1 (Will's admin QA): approved projects were landing BARE — GitHub description/topics/homepage existed on candidates but were never mapped at approval. Now: description→tagline (120 clip), topics→tags (normalized cap 8), homepage→demo_url; 6 live projects backfilled. Queue got per-source filter chips (demand ranking buried all demand-0 crawl rows below 100+ star-import candidates) + crawl runs deep-link to their filtered view. FINDING for P2 visuals: opengraph.githubassets.com/{any}/{owner}/{repo} hotlinks (200 image/png) — every repo has a social card; zero-cost card imagery per the avatar hotlink rule. Repo made PUBLIC by Will until near-launch (free Actions; fork-risk accepted — "our most valuable resources are users & content").
- 2026-07-23 · BOARD (Will): roadmap resynthesis. (1) Auto-approve → PUBLISH-ALL + retro-moderation ("human review time must never be the volume throttle"; low-star repos are dorkhub's soul — most user projects sit far under any threshold): AI moderation screen + user report mechanism + moderation queue; reversible automation (AI may unpublish, logged), irreversible (delete+block) stays human. Ships as P2.5 AFTER P2's quality floor (enrichment + og imagery) — nothing auto-publishes bland. (2) Lists → PUBLIC COLLECTIONS (per-list privacy toggle) in P3; GitHub lists API re-verified absent 2026-07-23, import stays "bring all stars, organize here". (3) Articles zone → P5 post-launch v1.1 (RSS-driven overview gallery, link-out-first so authors keep traffic, like/save parity, SEO engine; prior art remotebulletin was link-list-only). Vision principle 3 amended: quality is a FLOOR, not a throttle.
- 2026-07-23 · P2 plan (docs/plans/p2-discovery.md): og cards at natural aspect-[1200/630] uncropped (16/10 fill would clip GitHub's title/stats), plain lazy <img> + CardMedia onError fallback to the SVG placeholder underlay; FEED_COLUMNS exported as the single projection (saved/page.tsx mirror deleted); fixtures get repoFullName on the TYPE only (/design never hotlinks fake repos). Enrichment: Vercel AI Gateway (plain fetch, no SDK; default google/gemini-2.5-flash-lite; $5/mo recurring free credit, zero markup → $0 actual, no board gate), enrich when description OR topics missing (untagged = invisible to every tag-driven surface), human data always beats ai_* at approval, queue-review-first + best-effort inline fallback. Recs rail = client island + server-action-on-mount returning renderFeedCards output (documented rule-bend; keeps /home ISR-60 unpoisoned). /weird = force-dynamic route handler, one-off random OFFSET (documented exception to the no-OFFSET feed rule), nav Link prefetch={false}.
- 2026-07-23 · P2 QA corrections: GitHub og-images are 1200×600 (2:1), NOT the 1200×630 OG-spec default — card media aspect corrected to aspect-[2/1] (uncropped). GOTCHA: CSS aspect-ratio is only a PREFERRED ratio — an in-flow underlay (placeholder SVG, intrinsic 16/10) stretched the box via content min-height and silently re-cropped the og art; underlay layers must be absolutely positioned inside the aspect box. FINDING: opengraph.githubassets.com returns 200 + a generic GitHub card even for nonexistent repos (never 404s) — CardMedia's onError fallback is network-failure defense only; deleted/renamed repos degrade to GitHub's generic card, and syncProject name self-healing keeps those rare. Also: core rls_checks.sql policy inventory had gone stale at 0006 (star_imports policies flagged "unexpected") — inventory updated to 23; suites must run AFTER every schema-touching milestone, not just their own companion file.
- 2026-07-23 · P2.1 (Will's second admin QA): "enriched 0 — 1 failed" root-caused live. (1) Vercel AI Gateway FREE TIER 429s every model ("upgrade to paid credits" — probed flash-lite/deepseek/gpt-5-nano/nova-lite, all identical): the $5/mo-credit assumption was wrong without a top-up. Fix: GEMINI_API_KEY (Google AI Studio free tier, ~1k req/day, $0, OpenAI-compatible endpoint) now takes precedence in src/lib/ai/gateway.ts; AI_GATEWAY_API_KEY stays as the paid-credits alternative. (2) Batch-selection window bug: enrich fetched top-60-by-demand then filtered, but enrichable rows are demand-0 crawl candidates below that window — button said 52, batch saw 1. Now selects from the actual enrichable set (two lean queries merged, no .or()). (3) Run feedback: ?aireason= carries the first failure reason into the banner; "ai came up empty" row marker distinguishes attempted-but-empty from never-attempted. GOTCHA (probed live): supabase-js .eq('topics', []) typechecks but serializes to invalid `topics=eq.` — use .filter('topics','eq','{}'). Seed fixture projects (github_repo_id > 9e11, 8 published) unpublished on prod — gallery is 100% real content; fixture PROFILES stay (RLS-suite deps); full purge at P4 cleanup.
- 2026-07-23 · P2.2 (Will's third QA — "i love good feedback systems"): Google retired gemini-2.5-flash-lite for NEW api keys (completion 404 "no longer available to new users" while the model still LISTS in /models; the -latest alias 403s) — default is now gemini-3.5-flash-lite (probed 200 with Will's key). Enrichment UX rebuilt as EnrichRunner, a chunked client-driven loop (importStarsPage pattern, 5/chunk): idle states intent + count, running shows live tallies (tabular-nums, aria-live, layout-stable, deliberately unanimated per motion policy), stops surface the provider's actual reason with a resume button. STAMPING SEMANTICS FIXED: systemic failures (rate limit, provider error, missing key) stop the chunk WITHOUT stamping enriched_at — only genuinely-answered model replies stamp; the 12+3 rows dud-stamped by the 404 run were reset live. E2E proven on prod: apple/ml-mgie enriched via the real key ("instruction-based image editing using multimodal large language models" + 5 tags).
- 2026-07-23 · P2.5 wave set 1 SHIPPED (docs/plans/p2.5-self-running.md): publish-all + retro-mod (board, third confirmation — "add quality to mass quantity"). Auto-approved encoding = status 'approved' + decided_by NULL (zero schema change; humans always stamp decided_by); retro queue = approved ∧ decided_by null ∧ stars < AUTO_APPROVE_MIN_STARS (env, default 20). NO pre-publish content gate (deliberate — og card is the instant floor; AI fills within ~15 min; a hold would recreate the blocked feeling the board vetoed). Pipeline route (Bearer CRON_SECRET, 50s soft deadline under maxDuration 60): materialize ≤10 content-having-first + enrichNextBatch ≤8 at 4.5s pacing; driven by GitHub Actions cron 4,19,34,49 * * * * (secretless workflow, free on public repo; Vercel daily 9:07 fallback; at launch → Vercel Pro cron */15 and delete the workflow). Gallery default = trending (/newest new, /trending 308s home) per board. E2E live: 29 materializations across 3 runs incl. a concurrent double-fire (1 already_decided, 0 dupes, 0 strandings — supersede invariant + profile-race recovery held); enrichment continued rate-limit-free at pace. Import runner gained the "putting them on the wall…" phase. Retired copy: importDoneQueued ("we pick quality over volume"), sortRecent. NEXT P2.5 round: user reports + AI moderation screen — until then Will skims the retro queue every day or two.
- 2026-07-23 · P2.5.1 (Will's import QA: "felt like it took forever"): materialization cost was the fresh re-fetch + inline syncProject (README download) per item — pointless during import (snapshot is seconds old; READMEs aren't card content). MaterializeOpts gains trustFreshSnapshot (snapshot <1h → skip re-fetch; import-only; stale falls back to fetch) + skipSync (pipeline + import; human approve keeps full fidelity). Candidates don't snapshot homepage → computeSyncUpdate gains FILL-ONLY demo_url (null-only, never fights owner edits; forbidden-keys test matrix amended deliberately). Pipeline: MATERIALIZE_PER_RUN 10→25 (~1s/item), new pass 3 sync-backfill (last_synced_at null, 5/run) — starves behind enrichment until that backlog drains; daily sync cron nulls-first covers regardless (proven live: 189 published all carry READMEs, 116 demo_urls healed). Import phase 2 = FIRST SLICE (top 30 while you watch, ~15s) then "N more going live automatically" — the pipeline drains the tail. CardMedia retries a failed og load once after 1.5s (burst-load transients were latching cards onto the placeholder until next visit). NOTE: 189 published = launch content bar (150–300) already met.
- 2026-07-27 · P2.6 immune system SHIPPED (docs/plans/p2.6-immune-system.md; authored Fable-side as a handoff round — Opus/Sonnet execute P3-A next from the same master plan): user reports + AI moderation triage. project_reports + moderation_screens are DENY-ALL (0009; zero policies/grants — reports are forgeable/brigade-visible under client-write RLS, so the saves pattern was deliberately NOT reused; all writes via service role behind reportProject()/requireAdmin()/cron). AI is TRIAGE-ONLY this round: verdicts (ok/review/flagged + reason) label and order the admin queues; vision's reversible-automation ceiling (AI may unpublish, logged) stays deliberately unused until verdict quality is proven on real data. Budget rebalance holds the proven ceiling: ENRICH_PER_RUN 8→5 + SCREEN_PER_RUN 3 = 8 AI calls/run (768/day scheduled); screen runs as pipeline pass 3 BEFORE sync backfill (safety before cosmetics). Stamping discipline carried over: systemic failures stop without writing; genuine-but-unparseable replies stamp verdict 'review' with a synthetic reason (moderation_screens.verdict is NOT NULL — no null-safe fallback column exists, and silent-ok or infinite retry are both worse). Report constraints: unique (project, reporter) for LIFE (no re-report path, accepted v1), ≤5 reports/profile/24h, self-reports rejected, published-only. Retro screening filters on LIVE projects.stars_count (matches the admin retro section's population exactly — the candidate snapshot goes stale). gateway.ts ChatCompletionOk gained `model` (screen provenance needed it; nothing else exposed the resolved id). copy.ts gained its first nested object (reportReasons) — the /design/voice page now flattens one row per leaf.
- 2026-07-27 · P3-A lists SHIPPED (docs/plans/p3-lists.md; second round executed off the Fable handoff master plan, same session): public collections, GitHub-lists-familiar. DB `collections`/`collection_items`, ALL user-facing copy says "lists" (D7). RLS-first user-owned (saves pattern): select is_public-or-own, items require owner-of-parent AND published target; policy inventory 23→30. STABLE SLUGS: suffixed once at creation (≤5 attempts on 23505), renames never re-slug, `slug` EXCLUDED from the UPDATE column grant (asserted by name §2a‴ + behavioral T22). ROUTE-SHADOWING FIX: a project slugged `lists` would be unreachable under /u/[username]/lists — RESERVED_PROJECT_SLUGS added to generateProjectSlug (live data pre-flighted clean). GOTCHA (build-caught, tsc-invisible): 'use server' files may only export async functions — LIST_CAP/ITEM_CAP moved to src/lib/lists/policy.ts. GOTCHA (suite-caught): seed fixture projects were UNPUBLISHED on prod in P2.1, so behavioral tests needing a published target must resolve one at runtime, never hardcode b2000000-… ids. PostgREST count aggregate `collection_items(count)` probed live: one-element array [{count: N}]. toggleListItem is a real server action (cap check + revalidatePath; only the optimistic-UI shape mirrors toggleSave). Add-to-list = project-page dropdown (checkbox items; "new list…" Dialog renders as a SIBLING — Radix nesting race; creating from the dropdown auto-adds the current project). Private list = 404 to non-owners by RLS emptiness. No GitHub star-list import (no API). "appears in N lists" trending signal = P3-B note.
- 2026-07-27 · P2.7 HARDENING ROUND (Opus 5, off the Fable handoff plan; board scope = "fix what's confirmed broken, then stop"). Four defects found by an adversarial multi-agent audit of the P2.6+P3-A code and each re-verified by hand against source + prod before fixing; no migration, so both RLS suites are unchanged (re-run green anyway: 30 policies, 22 ingestion checks). (1) MODERATION PROMPT INJECTION: only the README was whitespace-collapsed (by htmlToText) — `description_md` and `tagline` are BOTH in 0001's authenticated UPDATE grant and writable over the REST API (no dorkhub UI exposes description_md), so their newlines could forge prompt structure; a forged `{"verdict":"ok"}` parses cleanly and is PERMANENT because screened rows never re-enter the retro queue. Fix: every untrusted field collapsed + per-field clipped (description 1000 « its 10k column budget), whole block fenced with a per-call random nonce the system prompt names as data (a fixed marker is forgeable — repo is public). (2) RETRO SCREENING STALLED — THIRD STRIKE of the window-vs-population class (P2.1 enrich, P2.6 retro, now this): one fixed `limit*3` window ordered decided_at asc with the already-screened filter applied in JS AFTERWARDS, so once those rows were screened it returned [] forever reporting `screened: 0` (indistinguishable from "nothing to do"); only an admin stamping decided_by released it. Latent on prod at discovery (4 eligible, 4 screened — fires above 9). Fix: the window WALKS via `.range()` paging — a deliberate scoped exception to the no-OFFSET rule, which governs user-facing FEEDS (cf. /weird), since this is an internal cron selection over a few hundred rows and a non-unique decided_at makes true keyset paging tie-skip; rows shifting mid-walk defer a row to the next run at worst, i.e. self-healing. The advance rule is extracted as `collectRetroPages` with an INJECTED page loader (the GitHub layer's fetchImpl idiom — no Supabase fake, which house conventions rule out) and pinned by tests. (3) FEED CURSOR VALIDATORS were `isString` for both elements while `buildFeedQuery` interpolates them into a PostgREST `.or(...)` — the exact grammar surface searchAll refuses .or() to avoid — reachable unauthenticated at /api/feed?cursor=. Probed live: an injected cursor reshaped the filter tree and a non-timestamp errored the query, BOTH answering 200 with an empty feed instead of cursor.ts's documented first-page fallback. Now uuid + ISO-8601 validated (character sets contain no grammar delimiters); re-probed live, all payloads fall back to page 1 and real pagination still advances. Blast radius was bounded (status=eq.published is a separate top-level AND; anon can't filter columns it lacks SELECT on). (4) PRIVACY/ISR COUPLING: list mutations revalidated the two /lists paths but never /u/{username}, whose public-lists section is ISR-cached off the anon client — so a list flipped public→private kept being served to anonymous visitors for up to 300s while the detail page 404'd correctly. All six call sites now route through revalidateListPaths incl. the profile path. Also: add-to-list's optimistic toggle had try/finally with no catch (a THROWN server action skipped the revert and escaped as an unhandled rejection — the toggleSave idiom it mirrors can't hit this, supabase-js returns {error} rather than throwing); AddToListControl rendered on the owner's own DRAFT where collection_items_insert_own guarantees 42501; both lists pages were per-viewer yet declared revalidate=300 justified by a comment citing SiteHeaderSession, a component that no longer exists (now explicit force-dynamic; the project page's identical stale comment corrected in place, its caching deliberately left to Fable as the highest-traffic page). NEW REGRESSION NETS (docs/plans/p2.7-hardening.md): source-scanning meta-tests for the two classes verify+test are structurally blind to — 'use server' modules exporting only async functions (build-only, shipped broken in P3-A), and RESERVED_PROJECT_SLUGS covering every static dir under /u/[username]/ (a sibling route would silently orphan a project's permalink). Both were proven to FAIL against injected instances of their bug before commit.
- 2026-07-27 · P2.7 wave H5 (second pass, after the full 46-finding adversarial audit landed). ADMIN RETRO WINDOW was the FOURTH sighting of window-then-filter: the human safety net for publish-all fetched newest-50 approved+undecided then filtered live stars in JS — probed live, exactly 4 of 200 qualify and they sat at ranks 43-46, i.e. ~4 more auto-publishes from the section rendering "none" permanently with no signal. Fixed by filtering in SQL on the EMBEDDED live `projects.stars_count` before the LIMIT (probed live first: PostgREST top-level filtering on an `!inner` embed works). Chose the embedded live column over the candidate's `stars_count` snapshot deliberately — the snapshot prefilter that screen.ts uses would narrow the window at the cost of a NEW blind spot (repos that lost stars since ingest), and screen.ts's own comment justifies its heuristic by saying the admin queue still catches that case, so making the admin queue use the same heuristic would have quietly voided that justification. FAIL-CLOSED CAPS: reportProject's 5/24h limit destructured only `{ count }`; a failed count returns null and `count ?? 0` read that as zero, skipping the only control on how many DISTINCT projects one account can report, exactly when DB pressure makes the failure likeliest. Same fail-open fixed in createList (LIST_CAP) and toggleListItem (ITEM_CAP). RLS BEHAVIORAL GAPS: neither suite ever exercised the `using` clauses of collections_update_own / collections_delete_own / collection_items_delete_own (§3a compares policy name+cmd, never the expression) while the list actions delegate authorization ENTIRELY to them (`.eq('id', …)`, no profile_id filter) — a policy widened to `using (true)` passed every check in both files. Added T23 (cross-owner rename/visibility/delete) and T24 (cross-owner item delete); rewrote T21's vacuous item assertion, which joined through `collections` so a private list's item disappeared because its LIST was hidden regardless of collection_items_select (widening that policy to `using (true)` still printed PASS while anon could enumerate a private list's contents over REST). Both new tests were NEGATIVE-CONTROLLED against deliberately widened policies inside the suite's own rolled-back transaction. That exercise exposed a worse meta-bug: psql ran past a raised RLS FAILURE and still printed "ALL CHECKS PASSED" (the notice sits after `rollback;`), so every green run since M1 was potentially masking a failure — both suites now `\set ON_ERROR_STOP on`, which is what makes the header's long-standing claim true. Also: OPEN_REPORTS_WINDOW shared between the screen engine (was 50) and admin reports (100) so reports 51-100 stopped being human-visible-but-AI-invisible; /api/me/lists imports LIST_CAP; both screen selections filter status=published; the engagement overlay CHUNKS at MAX_IDS_PARAM (a 400-item list silently dropped every id past the 100th — and 400 uuids overran Node's header budget, losing like/save state for the whole page); pipeline reports screenHasMore. DEFERRED to Fable with reasons (docs/plans/p2.7-hardening.md): flagged-verdict erasure by a later report (contradicts locked D5 — should `flagged` be sticky until a human resolves?), admin manual drain outside the AI budget (D3), pass-3 starvation + no per-call AI timeout (D3 ordering), screen-vs-admin ASC/DESC divergence (the P2.6 ADR's "match exactly" claim is false), home recs rail window (same class, low impact), and the design/copy polish set.
- 2026-07-29 · P3-B part 1 SHIPPED (docs/plans/p3b-rich-pages.md) — rich pages + the lists signal, back on Fable. Scope decided by a live content census, not preference: 207 published projects ALL have READMEs, but there are 0 lists, 1 claimed profile of 197, and every project published inside a 2-day bulk-ingest window. So author-supplied richness (screenshots, changelogs, description_md) has literally nobody to author it, while derived richness (README fidelity, honest recency, real avatars) pays off on all 207 pages today — this round is almost entirely the latter. D17: lists_count is a DENORMALIZED column, not a query-time aggregate; settled by a live probe returning PGRST118 for `order=collection_items(count).desc` — an aggregate can be displayed but can never order or keyset-paginate a feed, and it is RLS-scoped to the VIEWER, so the anon-cached card and the cookie-bound project page would disagree. D18: only PUBLIC list membership counts. The saves_count precedent (private rows, public counter) deliberately does NOT transfer — a save is one anonymous bit, whereas one account can hold 50 lists x 400 items, and at 207 projects "in 1 list" with no visible list discloses that exactly one person privately curated it. The number must also be backed by clickable evidence or it disagrees with the reachable set forever. Including private later is additive; excluding later is a visible drop, so start conservative. D19: count ROWS not distinct curators in v1 (keeps "appears in N lists" literally true); if it ever feeds trending, compute count(distinct profile_id) as a separate local value and never change what the column stores. D20: no compute_trending signature change — display-only (mirroring P2.6's TRIAGE-ONLY ceiling) avoids the 4-arg overload, the re-grant, the search_path re-pin, a 207-row rescore that would invalidate in-flight keyset cursors, and amending materialize.ts's hand-mirrored formula; tuning a weight against 0 lists is guesswork anyway. D21: recency now comes from a new github_pushed_at and projects.updated_at is no longer displayed — updated_at bumps on our own sync writes, so all 207 projects read "updated hours ago" regardless of the repo. GOTCHA the research missed and the migration had to solve: syncProject takes the 304 fast path for unchanged repos and writes NO metadata, so a newly added column stays NULL forever on every already-synced project — 0011 clears repo_etag (213 rows) to force one full re-fetch through the EXISTING daily cron; readme_etag is left alone since README bodies are the expensive half. Proven live: 207/207 filled, real activity spanning 2018-09-09 to 2026-07-29 (70 pushed this week, 65 dormant over a year), and the follow-up sync returned notModified:191 so ETags are back and the cost was one-time. D22 (resolving a P2.7 deferral): flagged verdicts NEVER auto-downgrade — screens upsert-overwrite (D5) and the cheapest way to trigger a re-screen is to REPORT the project you want de-flagged, so the safety net could be cleared by the very mechanism meant to raise it, with nobody having looked; the original reason is preserved (it is what a human must act on) while model/created_at advance so the same open report stops re-queueing. Escalation is untouched. D23: no index on lists_count until a "most listed" surface exists. Also: the admin retro window was the 4th window-then-filter sighting and the home recs rail the 5th (its window is now RECS_LIMIT + EXCLUSION_CAP expressed as arithmetic, since the exclusion set is the user's own saves and the candidates are the top-trending rows in the user's own tags — emptying was the COMMON case, not an edge case). README fidelity: prose.css styled h1/h2 ONLY and Tailwind preflight resets h3-h6 to inherit, so every `### Install` in the gallery rendered as undifferentiated body text; badges are now marked at sanitize-WRITE time (known hosts + any .svg) because CSS cannot select on rendered size, with author-supplied data-badge stripped first so a README can't opt its own screenshots out of the card treatment. Reading measure moved to the locked reference's 780px (~122 -> ~87 chars/line, measured live). Consent: the unclaimed badge string was hardcoded AND duplicated across the profile and claim pages — extracted to copy.ts with wording UNCHANGED (rewording a consent surface is a Will decision, not a refactor) and paired with the "is this you?" -> /claim link the master plan specifies, since a badge with no route to act on it is disclosure without agency. 196 unclaimed profiles backfilled with their public GitHub avatars (derived from the immutable numeric id — no API call, $0, host already allowlisted); the backfill's own pre-commit verification ABORTED on the first attempt because the check was stricter than isAllowedAvatarUrl's real host-only rule and rejected the one pre-existing `?v=4` row — exactly the check-vs-policy drift the guard exists for.
- 2026-07-29 · P3-B part 2 SHIPPED (docs/plans/p3b-search.md) — search quality, /search, facets, plus Will's two P3-A QA fixes. D24: projects.language_slug = generated lower(primary_language). A naive slugify COLLIDES — probed live, `C#` and `C++` both become `c-` — while lower() is collision-free across all 23 live languages, and it is exactly what the existing filter already expected. That filter had been DEAD since M5 and failed SILENTLY (empty page, not an error): resolveFeedFilterSpec lowercases the incoming value for URL/cache-key hygiene and buildFeedQuery then exact-matched the GitHub-cased column, so /api/feed?language= returned 0 rows for typescript, TypeScript, python AND Python against 76 published TypeScript projects — even correct casing failed, because the code lowercases first. Now 48/20/11 on prod. Dropped idx_projects_language (it only served the removed exact-case filter; keeping it would be pure write amplification at 4 pipeline writes/hour). D25: search results are capped top-N by relevance, NOT keyset-paginated — the no-OFFSET rule governs FEEDS, and a set merged from independently-ranked legs has no single ordering key to cursor on. D26: relevance scored in TS through mergeSearchHits' already-injectable rankOf; pushing similarity()/ts_rank into PostgREST would need an RPC and re-open the surface the .or() ban exists to close. The scoring fix matters because ranking was POPULARITY, not relevance — an exact name match sorted below a popular project whose tagline merely contained the substring. Tiers (name exact > prefix > contains > exact tag > repo/owner > tagline) are spaced 5 apart and popularity is NORMALIZED to [0,1] across the candidate set: raw trending_score is ~39,661 with a spread under 2, so an un-normalized term would swamp every tier and reproduce the original bug. Proven on live data — q=cli puts all four projects literally NAMED "cli" above simple-shadcn-cli, which has a higher trending_score. Scored from the ROW against q rather than from which leg produced it, since leg provenance is lost to first-seen dedupe. D27: /search is a STATIC shell + client island reading useSearchParams under Suspense — reading searchParams in an RSC forces per-request dynamic rendering forever on a public route with an unbounded space of distinct query strings, the most plausible way to blow the $0 pre-launch posture; build emits `○ /search`. Cost named: client-rendered results mean nothing for crawlers, which is free while robots is noindex. D28: search stays demoted (vision principle 1) — icon-only trigger, no home search bar, no nav entry; /search is discovered via the palette's "see all results" row. D29: NO rate limiter — /api/search is public and now runs eight legs, but a real limiter needs shared state (new spend = board gate); mitigations are bounded q, fixed leg count, server-clamped limit and the CDN cache, recorded as a known gap rather than papered over. FACETS (language/tag/stars/demo) apply IN SQL inside each leg before the LIMIT, never to the returned set — proven live: for q=cli, Java has ZERO rows in the unfaceted top-48 yet ?lang=java returns one, which a post-filter could not do. That is the sixth encounter with the window-then-filter class, this time avoided by design. Refinement chips are derived from the UNFACETED response and remembered, and an active facet always renders a chip even when absent from that vocabulary — caught in the browser, where a shared ?lang= link applied the filter but rendered nothing to remove it. Pure facet helpers live in src/lib/search/facets.ts, NOT queries.ts, because the client island needs collectFacetOptions as a value and queries.ts is server-only. Also deleted the command palette's hand-written mirror of SearchResults (already desynced) in favour of `import type` from the server-only module — erased at compile time, verified by a clean production build. D30 (Will's QA): the list visibility switch was ambiguous BY CONSTRUCTION — checked={isPublic} with a label swapping public/private, so the label named STATE while a switch label should name what it CONTROLS; "off / private" read as "private is off, therefore public", and flipping it showed "public". Now a fixed `public` label with the switch position carrying state, plus a helper line when off that also states the D18 promise (private lists don't count toward a project's list total) where the choice is actually made. And the add-to-list trigger now shows the viewer's OWN membership count at zero extra cost (the control already fetched /api/me/lists on mount to render checkmarks); the GLOBAL lists_count was removed from the project page action row per Will, since two similar counts side by side read as a contradiction — the global signal still ships on ProjectCard where browsing happens.
- 2026-07-30 · P3-C SCALE HARDENING SHIPPED (docs/plans/p3c-scale.md; Opus 5 executing the Fable-approved plan; migrations 0013-0016). Everything measured at 10k/100k in rolled-back prod transactions, harness committed as supabase/tests/scale_probe.sql (D36). HEADLINE: the feed's keyset pagination never actually sought — PostgREST `.or()` can't emit a row comparison, so deep pages scanned-and-discarded (Rows Removed: 7001, O(offset)); D31 feed_page RPC (dynamic SQL from constant fragments + USING binds — static plpgsql goes generic and degrades back to a filter; typed args replace the last interpolated filter string), D32 SECURITY INVOKER (anon provably sees zero drafts). 3.57ms→0.64ms full-shape at 10k, flat with depth; zero-mismatch parity proven old-vs-new incl. cross-implementation cursor interchange on live HTTP. D33 AI budget = ai_usage DB ledger + atomic claim_ai_call, fail-closed at every layer (in-memory counters enforce nothing across serverless instances); all three chatCompletion paths claim first — closes P2.7's "admin drain outside the budget". D34 import/moderation cron split (/api/cron/ingest vs pipeline) — resolves pass-3 starvation structurally; sync deadline-governed ≈5,800/day (10k refresh 50 days→~2). D35 /weird uuid-pivot O(log n) (retires its OFFSET exception). D37 REGRESSION CAUGHT: generated columns are NULL on NEW in BEFORE triggers, so 0012's language_slug made every counter bump read as an edit — updated_at churned on every like since that morning; fixed in 0016, pinned by negative-controlled T26. D38 ingest materializes demand-first (stars-only order = 267-318ms parallel seq scan at 100k; index order 0.1ms; also matches what admins see). Storage 49MB/500 corrected from "very near limit"; dbSizeMb in every pipeline response, warn at 400.
- 2026-07-30 · P3-D GUARDRAILS + POLISH SHIPPED (docs/plans/p3d-guardrails.md; migration 0017; tags p3b+p3c applied on Will's green light). D39 the AI budget is DOLLAR-honest: board directive "~$5 max or $1 daily" → implemented BOTH (belt+braces): AI_DAILY_MAX 800 ≈ ≤$0.48/day + NEW AI_TOTAL_MAX 5,000 lifetime ≈ ≤$3, from verified worst-case ≤$0.0006/call (README prompt input clipped 4k chars; 2× price margin) — claim_ai_call(p_max, p_total_max) enforces the total under the same day-row lock (race-free, past days immutable); 0 = kill-switch writing nothing (I14e + wire-proven); refusals name the tripped ceiling; pipeline reports aiCallsToday/aiCallsTotal. D40 per-call timeout: AbortSignal.timeout 30s (AI_CALL_TIMEOUT_MS) — a hung metered call costs one slot, not a window; rides the existing error path (engines stop without stamping). D41 list item counts count the RLS-VISIBLE membership (id-only projects!inner embed, counted client-side) so the number is by construction what that viewer's detail page renders — proven mismatch was naive 2 vs visible 1 with a drafted member. D42 screen-engine ASC vs admin-retro DESC is INTENTIONAL (same population, different audience: drain oldest debt vs show newest exposure) — documented at all three sites; do not align. D43 motion sweep verdict: Approve, zero findings — repo-wide scan clean (no transition-all/ease-in/raw-color/>300ms/keyframes-on-rapid/motion-lib imports); reduced-motion kill switch covers 100% of motion by construction since all motion is CSS. Tooling note: the browser pane's synthetic clicks silently miss at non-1:1 screenshot scale (event-probe proven) — verify handlers via direct DOM dispatch before diagnosing the app.
- 2026-07-30 · P4 L0 SHIPPED (Will approved the purge live): first VERIFIED prod backup (docs/ops-backup.md, restore proven 17/17 table parity) → both RLS suites refitted to SELF-PROVISION (D44 — zero seed.sql dependency, green before AND after; throwaway github_ids 991…/9900000009xx sit outside GitHub's real id space, unlike the fixtures they replace) → fixture purge executed in one guarded transaction (5 profiles / 9 draft projects / 2 featured_slots / 1 claim_invite + seeded engagement; gallery intact at 207; the fabricated-real-range github_id CLAIM HAZARD — a stranger could have claimed mollybuilds — is closed, unexploited). seed.sql stays for local dev; scale-probe comments updated.
- 2026-07-30 · P4 L2 SHIPPED. D45: profiles' API SELECT grant is COLUMN-LISTED (0018) — is_admin was publicly enumerable since 0001; user_id deliberately KEPT (the signed-in shell filters .eq('user_id',…) under API roles and PostgREST ties filtering to SELECT). CAUGHT LIVE: SQL `*` needs every column, so the four pages star-selecting profiles 404'd for ~10 min post-apply — RULE: profiles may never be star-selected under an API role (PROFILE_COLUMNS/ProfileRow, src/lib/profiles/columns.ts); grant changes get an app-projection grep BEFORE applying. D46: per-IP search rate limiter (0019) clones the ledger pattern with the OPPOSITE failure direction — fail-OPEN, availability wins on a read-only public endpoint, only a literal false 429s; wire-proven 60×200→3×429; both search UIs name the limit instead of faking "no results". D47: CSP ships REPORT-ONLY first (CSP_ENFORCE=1 flips), script/style keep 'unsafe-inline' (nonces cannot live in ISR-cached HTML; RSC bootstrap is unhashable), img-src stays https:-wide ON PURPOSE (sanitized README bodies embed arbitrary https images), connect-src + the violation report-uri derive from the Supabase/Sentry env vars, and headers live in next.config.ts — never proxy.ts (auth stays scoped). D49: Sentry code inert-until-DSN; Will: DEDICATED account, separate from partyreel. Audits: advisors zero-new (documented accepted-by-design set); pnpm audit 4 transitive Next build-tool findings accepted (no next/image by cost rule; unfixed at latest Next).
- 2026-07-30 · P4 L1+L3+L4 SHIPPED. D48: featured slots MECHANISM ONLY (board) — /admin/featured CRUD, FeaturedStrip of REAL cards labeled ON THE CARD (sponsor_label or 'featured' — a screenshot of one card is still honest), page-1 DISPLAY dedupe only (the keyset cursor derives from the unfiltered page so pagination never gaps), published-only enforced twice (create check + the !inner embed dropping invisible targets, so a moderated-away feature vanishes by construction); T29 pins the window matrix; E2E on prod data: 1 labeled card, 0 organic duplicates, 24 total. D50: /search carries page-level noindex that SURVIVES the launch robots flip (client-rendered thin results); sitemap went dynamic (1,103 URLs exact = 10 statics + 207 projects + 197 profiles + 689 tags), JSON-LD via lib builders (spec-exempt from script-src), SITE_URL single-sourced (src/lib/site.ts), real 404/error pages wire the long-orphaned copy, /terms + /privacy are DRAFTS for board review. L4 closed the P2.7 polish ledger: list dead-ends (exhausted-slug names the cause; deleteList failure stays in context; EditListForm per-field reporting + saved state + throw-catch), surrogate-safe reason clip, admin ' · unpublished' parity, flagged-chip mono treatment, aria-hidden kickers, profile-rail idiom alignment, owner-named lists <title>.
- 2026-07-30 · P4 CONTENT + OBSERVABILITY LIVE (Will's morning directives). BIG IMPORT EXECUTED on the approved seed list via the new headless runner (scripts/run-crawls.ts — same lib orchestration as /admin/sources incl. crawl_runs bookkeeping; 'server-only' resolved via NODE_OPTIONS=--conditions react-server): 42 topics + 10 awesome lists → 3,792 candidates created, 408 dedupe-touched, 0 blocked; ALL at min-stars 50, deliberately over AUTO_APPROVE_MIN_STARS=20 so this wave adds ZERO retro-screen load — and the star threshold gates SCREENING, never drain rate: publish-all materializes 40/tick × 96/day ≈ 3,840/day regardless, so the 3,733-pending hopper drains in ~23h. SENTRY LIVE on the DEDICATED dorkhub org (dorkhub1@gmail.com; project dorkhub/dorkhub, created via Will's authorized Chrome session; NEXT_PUBLIC_SENTRY_DSN in Vercel env, redeployed) — deployed CSP now carries the ingest origin + report-uri, proving the env-derivation chain. CSP BURN-IN'S FIRST CATCHES: (1) vercel.live script/frame blocks in Will's real Chrome = the Vercel team toolbar, admin-session-only — resolve at the enforce step by DISABLING the toolbar on prod (no allowlist-weakening for an admin-only widget); (2) a same-origin RSC-prefetch "violation" reported ONLY by the Electron in-app pane = tooling artifact, no policy change (real Chrome: zero connect-src reports). EMAIL: dorkhub.com is registered AT Cloudflare with CF nameservers → Cloudflare Email Routing is the free path (hi@dorkhub.com → dorkhub1@gmail.com); blocked on Will's login (credential boundary) — 3-step morning task; terms/privacy contact swaps after. BOARD STEER RECORDED: L5 gets a DEDICATED planning round; bar = "as close as possible to what the final product should feel like"; many targeted-improvement rounds expected pre-real-launch; Supabase new-signups toggle is an accepted lever for building at production level while closed.
- 2026-07-30 · BOARD RAMP DIRECTIVE (Will, live): pre-users, fill the library fast using ANY capabilities — internal cron pacing is not a constraint at this stage. Executed shape: sourcing rides the SEPARATE search bucket (30/min, 1k-cap/query) at up to 400-1000/topic; crawl→drain PIPELINING makes materialization ~API-free (trustFreshSnapshot hits on <1h snapshots — tonight's first drain paid ~1 core req each only because snapshots were 10h stale); the 5k/hr core budget is reserved for the real consumer, README/metadata backfill (scripts/bulk-sync.ts, rate-aware, resumable via the M4 stamping rule). Bulk drivers stay on the SAME lib functions as the crons (blocklist/sticky/quality identical); crons remain the hands-off steady state. STANDING CHECKPOINT: watch dbSizeMb between waves — the 500MB free tier caps somewhere ~12-35k projects depending on real README weight; bring Will the Supabase Pro call with measured numbers, not projections.
- 2026-07-30 · D51 USERNAME POLICY = GITHUB'S FULL LEGAL ENVELOPE (board, Will live): "I don't want us to have to restrict anyone with github-allowed usernames." The 0001 pattern was GitHub's NEW-SIGNUP rule and rejected real legacy logins (LingDong- trailing hyphen, Rob--W consecutive hyphens, f single char) — blocking both their repos at import AND the humans themselves at claim time. Migration 0020 + USERNAME_PATTERN widen in lockstep (same commit, must never drift) to 1–39 chars, [A-Za-z0-9-], alnum START required — leading hyphen stays banned (GitHub never issued one; flag-shaped names are an injection footgun); charset/length unchanged so the URL-safety envelope is identical, strict superset so existing rows pass by construction. The 19 invalid_username sticky-rejections reversed to pending; the 2 valid-username stragglers (moshang-ax/lottery, dawid-szewc/perplexity-cli) turned out to be a DIFFERENT wedge — profile exists w/ matching github_id, zero projects, no slug/repo-id collision in current state — flipped back for a live-stderr retry under the new termination guard (contained if still wedged).
- 2026-07-30 · D52 BOARD GATES CLEARED (Will live): TERMS/PRIVACY content approved ("look good"; contact literal swaps to hi@dorkhub.com once routing lands — three pages now share it: terms, privacy, /sponsor). FEATURED LABEL concept approved for future sponsorships (default on-card label 'featured'); NEW /sponsor marketing page shipped in the draft-for-review pattern — mechanism-honest, deliberately NO pricing (D48 mechanism-only stands; page opens an email thread instead). COLOPHON reshaped per board: 'built' = personal credit (Will Gibson → willgibson.com + AI agents; willgibs.com 404s, pending Will's URL confirmation), references row REMOVED (moodboard sites never built from); transitions.dev motion attribution KEPT — docs/motion.md proves genuine adapt-don't-vendor usage, deleting it would be the dishonest edit.
- 2026-07-31 · D53 FEATURED GOES INLINE + EMAIL LIVE (Will's review round). (1) Featured placement: IN the gallery grid, not a band above it — Will: a separate row "feels easily skippable... like ads"; FeedSection now composes ONE grid (featured cells first, featured variant + on-card label, organic after with staggerOffset so the entrance sweep stays continuous); FeaturedStrip deleted; page-1 display dedupe + untouched-cursor semantics carried verbatim; live-verified against his real willgibs/linkflow slot. (2) /sponsor v2: inline language + actionable (include-list, mailto CTA subject 'featured slot', reply-in-days expectation); still NO pricing (D48 stands). (3) EMAIL LIVE: I drove his authorized Chrome — Cloudflare Email Routing rule hi@dorkhub.com → hi@willgibs.com (HIS verified destination, chosen over the dorkhub1 proposal by his own 20h-old verification) Active; MX/SPF/DKIM added via 'Add missing records', dig-confirmed propagated; contact literal swapped on terms/privacy/sponsor in the same deploy. (4) Colophon: footer link dropped (section, not destination — reachable by scroll on /manifesto).
- 2026-07-31 · D54 $0 LAUNCH POSTURE (board, Will live): "defer pro upgrades until absolutely required... ideally runs flawlessly at scale for free" — SUPERSEDES 2026-07-21's Pro-at-launch line. NO Vercel Pro at launch: GitHub Actions stays the PERMANENT */15 scheduler (free on the public repo; curl targets stay pinned to the dorkhub-ten.vercel.app alias ON PURPOSE — decouples ingest/moderation from new-domain DNS health), vercel.json's two daily crons remain the fallback, Spend Management moot (Hobby can't bill — it pauses), domain attach + Web Analytics ride Hobby's free tier. PRO-TRIGGER LEDGER (watch, don't act): (1) first PAID featured slot ⇒ upgrade FIRST, same day — Hobby ToS is non-commercial and is satisfied today only because nothing is sold (D48 mechanism-only); (2) Hobby resource ceilings — watch Vercel Usage weekly the first month (bandwidth 100GB/mo, analytics 50k events/mo; Vercel warns before hard stops); (3) chronic non-inactivity GH Actions failures ⇒ Pro's sub-daily crons (the 60-day inactivity auto-disable is reset by any push; observed today: GitHub throttles scheduled runs to ~90-min gaps under load — the daily fallback + complete content thread make this tolerable); (4) Supabase Pro separately at 300 MB or PITR-worthy UGC. Also recorded: full Chrome grant for Vercel/Cloudflare/Search Console launch clicks with ACCOUNT-IDENTITY CHECKS (Cloudflare = hi@willgibs ✓ verified in-session; Search Console = dorkhub1@gmail.com ✓ session existed, switched, never entered credentials); robots flip AT cutover (not held for a later GO).
- 2026-07-31 · D55 LAUNCHED — dorkhub.com LIVE at $0 (P4 L5 executed same-session as D54). Sequence as-executed, each step verified: (0) 70MB pg_dump snapshot; verify+tests+build green; auth-config baseline JSON saved outside the repo; dig baselines (CAA empty; apex+www+wildcard A pointed at a DEAD Oracle parking stub from Mar 2025 — repointing unambiguous). (1) vercel.live toolbar OFF for Production (the only real burn-in violator). (2) CSP ENFORCED: burn-in review showed only the known set (vercel.live ×2, Electron same-origin artifact, our wiring test); CSP_ENFORCE=1 Production-scoped; redeploy; header verified non-Report-Only; 4-page click-through clean in real Chrome. (3) DOMAIN: Vercel prescribed per-project CNAME 302eae8fc9b160c9.vercel-dns-017.com for BOTH apex and www (new-IP-range flow, not legacy 76.76.21.21); Cloudflare zone edits = delete 2 stale A records, add 2 CNAMEs grey-cloud/DNS-only (apex CNAME flattening coexists with MX — mail records untouched, dig-verified) + GSC TXT in the same visit; cert issued; apex 200 w/ enforced CSP + HSTS; www 308→apex; alias still 200. (4) Supabase auth: site_url → https://dorkhub.com via Management API (allowlist already held all four origins — left untouched); GET-echo verified; REAL OAuth round-trip on dorkhub.com lands signed in (cookies are per-host, so this exercised the full loop). (5) ROBOTS FLIP (allow + layout noindex removed) + per-route canonical `alternates:{canonical:'./'}` (NEW: no page emitted a canonical and the vercel.app aliases keep serving — every page now claims its dorkhub.com URL) + @vercel/analytics (same-origin beacon, no CSP change; makes /privacy's existing claim true) + comment hygiene (pipeline.yml header = permanent scheduler; terms/privacy DRAFT banners → approved). (6) GSC: Domain property verified via DNS TXT on the dorkhub1 account (manual token path — DECLINED Google's OAuth-into-Cloudflare auto-flow, out of grant scope); sitemap submitted, "submitted successfully". (7) Sentry Uptime monitor on https://dorkhub.com/ (dorkhub org, free tier, 1-min interval, production env) — pre-create test check PASSED. (8) Backups: scripts/backup-prod.sh + launchd agent com.dorkhub.backup (Sun 09:00, runs-at-wake) smoke-tested end-to-end (80MB dump + checksum + prune-to-8 + log). LAUNCH-VERIFY CATCHES (the reason verification is a step): (a) layout-relative canonical resolves to '/index' for the root route in prod builds only — both home pages pin canonical '/' (the /home render at '/' had claimed '/home'); (b) sitemap silently truncated to 3,011 URLs — PostgREST's 1,000-row cap, invisible when D50 shipped under 1k rows (7th window-class sighting); (c) un-truncated file hit 55,679 — OVER the 50k sitemap spec cap — profiles now = published-owners only and tags need ≥3 published projects (AI enrichment's 15,656-tag long tail is thin content); (d) the embed-filtered profiles walk cut off at exactly 4,000/14,020 — an inner-embed filter makes .range() pages short WITHOUT meaning exhaustion (8th sighting, pagination-layer form) — profiles now DERIVED from the already-fetched project rows (zero extra queries, consistent by construction). Final sitemap = 36,202 URLs, arithmetic-exact (11 + 16,972 projects + 14,020 profiles + 5,199 tags). OG image 200 image/png on the real domain; GH Actions crons green post-window; analytics enabled (free Hobby tier). Announce/claim invites = Will's move, on his clock.
- 2026-07-31 · D56 U1 VERDICT — ABYSS ADOPTED, TYPE KEPT ("Quiet dev-native · abyss"). Round shape: R1 four-skin bake-off on real prod content at /design/directions (warm-terminal, paper-zine, electric-depth, control) → R2 Will: finalists current + electric-depth, HARD NO warm-terminal/paper-zine (skins deleted; warm/paper palettes settled OUT) → R2.5 six-skin finalist families (current/deeper/sharper + electric/abyss/violet) → R3 Will: "abyss is my favorite... but with our current typography (no swap)." EXECUTED: abyss dark+light token blocks landed verbatim in globals.css minus --font-display (Instrument Sans/Geist/JetBrains Mono untouched; Outfit deleted with its route-local layout); radius 0.45→0.55rem (part of the approved look — shape, not type); blue-black neutrals hue≈267-268, primary oklch(0.87 0.14 198) electric cyan, links go VIOLET oklch(0.8 0.12 292) dark — the violet undertow is the one deliberate identity addition. Zero component changes (every surface reads vars — the U1 mechanism was the adoption path). og-tokens.ts re-sampled from live computed styles (canvas getImageData; browser does oklch→sRGB gamut mapping) — feeds OG images/icons/manifest; the "re-sample on theme change" rule is now written at the source. /design/directions rebuilt as round closer: control = product, one legacy skin (pre-U1 values) for before/after; page + directions.css get DELETED when Will's done comparing. Motion untouched — palette-only adoption, no review-animations round. Micro-details preserved by construction (they re-tune through vars, never disappear). QA: dark+light × home/feed/project/design, theme-toggle round-trip, console clean, verify + 761 tests + prod build green.
- 2026-08-01 · D57 U2 ADOPTION — the exemplar became the product (board: "Amazing work, both approved!"). Executed as A1-A5 after two review rounds (R2 forks + a 14-item second pass, both already shipped to the previews). (1) FEED PLATFORM: 'active' (migration 0021's sort) reached the app — FeedSort gained it, sharing the RecentCursor codec over github_pushed_at; verified seeking on live data (page 2 continues strictly below page 1). Latent fix found while wiring it: toFeedPage encoded a cursor from a possibly-absent timestamp, which decodes back to null and silently RE-SERVES PAGE 1 — a missing value now ends pagination instead. Rhythm shipped as an opt-in `leadSpan` option on renderFeedCards rather than a parallel component, so the six other card consumers and every load-more page are untouched. FeedFilters rebuilt: chips are real Links (sort/tag stay in the URL — M5 decision 1), the sliding pill measures the ROUTE-active chip with an optimistic target so it moves on click, and the reset-on-prop-change is done during render (React's documented pattern) rather than in an effect. NEW /active route + sitemap entry; tag routes deliberately keep trending/newest only. (2) COMPOSITION: preview trees moved to permanent homes ((app)/_sections, (app)/_discovery, components/site-footer); is-isnt-strip + manifesto-teaser retired into how-it-works and the footer; previews + harness + u2-preview.css deleted with their keyframes graduated into globals.css. SiteFooter is now the multi-column close, presentational with an OPTIONAL stats prop (components/ stay fetch-free; layouts pass the cached getPlatformStats, 404 omits it so a miss costs no query). Metadata repositioned to the board-picked H1 — root title/description AND the three OG images + manifest all still carried 'a home for the things you build for fun'. (3) 404 BUG (pre-existing, found in QA): every in-app notFound() rendered the whole shell TWICE — the root not-found supplies its own chrome but ALSO renders inside (app)/layout.tsx. Fixed with an (app)-group boundary that is content-only; both share NotFoundContent. (4) MOBILE NAV (the board's one note): header is responsive — one row under `sm` (wordmark · search · menu), which is what made sticky viable at every width; the menu is a Sheet (Radix gives focus trap, Escape, scroll lock, overlay) with staggered items; useHeaderAuth extracted so the desktop dropdown and the menu share ONE resolver/cache/link-list. (5) MOTION: transitions.dev skill installed at the board's suggestion and adapted onto our tokens (never vendored) — spinning counter (CounterReel), skeleton→content reveal, error shake, load-more text swap, search clear dissolve, menu stagger; ⌘K stays unanimated. Self-audit against review-animations' escalation triggers passed (the skill is disable-model-invocation, so it was applied as a checklist, not invoked); the hero's 700ms deal-in is now a DOCUMENTED marketing-surface exception in motion.md rather than an implicit one. AvatarBadge fixed the "card avatars never load" bug at its root (the row mapper dropped avatar_url) with a zero-JS layered fallback that also covers dead URLs. STILL TABLED with board agreement: list-any-repo-by-URL (needs the safety pass — route through ingest + moderation, never direct publish) and the broader nav IA (lands with the pages that deserve nav weight).
